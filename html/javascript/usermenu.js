function editUserStart(username) { // startup function to make the page look correct
    document.getElementById("user").value = username;
}
function userInfo(username) {
    document.getElementById
}